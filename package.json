{
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "cd dist && del * /S /Q && npm run build && gh-pages -d dist"
  },
  "homepage": "https://pc810.github.io/dwl-sphere",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1",
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "three": "^0.121.1"
  }
}
